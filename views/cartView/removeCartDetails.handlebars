<div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom box-shadow">
<h1 class="my-0 mr-md-auto font-weight-normal">Stevens Marketplace</h1>

<nav class="my-2 my-md-0 mr-md-3">
    <a class="p-2 text-dark" href="/home">Home</a>
    <a class="p-2 text-dark" href="/newListing">New Listing</a>
    <a class="p-2 text-dark" href="/cart">Cart</a>
    <div class="dropdown">
        <button class="dropbtn"><img src = "../../public/images/profile icon.jpg"  width="25" height="25" alt ="Profile"></button>
        <div class="dropdown-content">
        <a href="/profile">Your profile</a>
        <a href="/yourListings">Your listings</a>
        </div>
    </div>

</nav>
<a class="btn btn-outline-danger" href="/logout">Logout</a>

</div>

<p class = "message">{{message}}</p>


<div class="jumbotron">
  <h1 class="display-4">Hello {{user.username}}</h1>
  <p class="lead">You have this in your cart: </p>
</div>
<div class="searchResults">
      <h4 class="d-flex justify-content-between align-items-center mb-3">
        <span class="text-muted">Your cart</span>
        <span class="badge badge-secondary badge-pill">{{productData.length}}</span>
      </h4>
         <!-- Cart Info --> 
       <ul class="list-group mb-3">

         {{# each productData}}
         <div class=""> 
          <li class="list-group-item d-flex justify-content-between lh-condensed">
          <div>

            <h6 class="my-0">{{this.productName}}</h6>

            <p class="text-muted">{{this.description}}</p>
            <a href="#" class="btn btn-danger" onclick="this.href='/removeCart?id={{this._id}}'">Remove Item</a> 

          </div>
            {{!-- <a href="#">X</a>  --}}
          <span class="text-muted">${{this.price}}</span>


        </li>

         </div>


        {{/each}}
        <!-- Promo Code --> 
        <li class="list-group-item d-flex justify-content-between bg-light">
          <div class="text-success">
            <h6 class="my-0">Promo code</h6>
            <small>GIVE-US-AN-A+</small>
          </div>
          <span class="text-success">-$5</span>
        </li>
        <!-- Total --> 
        <li class="list-group-item d-flex justify-content-between">
          <span>Total (USD)</span>
          <strong>${{total}}</strong>
        </li>
      </ul>


<div class="btn-toolbar" role="toolbar" aria-label="Toolbar with button groups">
  <div class="btn-group mr-2" role="group" aria-label="First group">
    <a href="/home" class="btn btn-danger">Continue Shopping</a> 
  </div>
  <div class="btn-group mr-2" role="group" aria-label="Second group">
    <a href="/checkout" class="btn btn-danger">Proceed to Checkout</a> 

  </div>
</div>
</div>

<script src="public/js/removeCart.js"> </script> 
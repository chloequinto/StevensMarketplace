<table>
    <tr>
        <td>
            <section>
                <div class="container">
                    <img src="{{productData.image}}" class= "productImage" onerror="this.src='../../public/images/none.png'"/>
                    <p>
                        {{productData.productName}}
                    </p>
                    <p>
                        {{productData.description}}
                    </p>
                      <a href='' onclick="this.href='/checkout?id={{productData._id}}'" class="btn btn-danger" id="likeID">${{productData.price}}</a>
                   
                </div>
            </section>
        </td>

    </tr>
    
</table>

